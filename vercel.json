{
  "version": 2,
  "framework": "vite",
  "installCommand": "npm install --prefix frontend",
  "buildCommand": "npm run --prefix frontend vercel-build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
